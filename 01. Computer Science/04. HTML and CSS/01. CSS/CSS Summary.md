

## **Wednesday** {#wednesday-5}

CSS [Vertical-align](https://developer.mozilla.org/en-US/docs/Web/CSS/vertical-align)

CSS [Text-align](https://developer.mozilla.org/en-US/docs/Web/CSS/text-align)

CSS [Transforms](https://developer.mozilla.org/en-US/docs/Web/CSS/transform) are for when you want to rotate, scale, skew, or translate and element

- Utilizes transform:
- Only works on elements governed by the CSS box model with exceptions

CSS [Translates](https://developer.mozilla.org/en-US/docs/Web/CSS/translate)

CSS [Transitions](https://developer.mozilla.org/en-US/docs/Web/CSS/transition) are for when an element changes state do to interaction

- Utilizes transition:
- Different states may be defined using pseudo classes ie :hover
- Different states may be defined dynamically using js

CSS Animations are for when an element changes state on load in

- Requires @keyframes
- Utilizes animation: theAnimationName
- Effect name is customized

**My Commonly Used CSS Properties:**

visibility:
opacity:

clip-path:
white-space:

object-position:
object-fit:

overflow-x:
overflow-y:

box-sizing:
box-shadow:

vertical-align:
text-align:

justify-items:
justify-self: (grid)

display:
position:

align-items: (flex)
justify-content: (flex)

gap:
float:

width:
height:

max-width:
min-width:

max-height:
min-height:

scale:
rotate:

resize:
z-index:

isolation:
mix-blend:

margin:
padding:

color:
font-family:

font-size:
font-weight:

text-decoration:
letter-spacing:

text-transform:
text-rendering:

animation:
transition:

cursor:

filter: blur()
backdrop-filter: blur():

background:
border:

**My Commonly Used At Rules**

@media
@import
@keyframes

**My Commonly Used Pseudo Classes**

:root
:hover
:active
:visited
:focus
