# Officegen

**npm:** `officegen`

Multi-format Office document generator. Creates .docx, .pptx, and .xlsx files. Older and less actively maintained but covers all three formats in one package.

**Use cases:**
- Simple document generation across multiple Office formats
- Legacy projects needing a single multi-format library

**Install:**
```bash
npm install officegen
```
