## Why Emitters

Emitters allow for loose coupling and are similar to a pub sub.

In this way the emitter allows for a 1 to Many action whereas calling functions directly is 1 to 1.

Also this permits logic to be scattered across different parts of the project as needed.

And emitters facilitate dynamic subscriptions in which handlers can be turned on or off at runtime.
