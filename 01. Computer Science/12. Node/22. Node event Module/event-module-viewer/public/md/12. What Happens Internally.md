## Emitters Under The Hood

What Happens InternallyWhen .emit() is called, the system performs three fast actions:

Lookup: It searches its internal registry for the matching event name.Payload

Delivery: It passes any extra arguments you provided directly into the callback functions.

Execution: It loops through and runs each registered handler function one by one, synchronously.



