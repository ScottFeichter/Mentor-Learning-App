## Database in the Abstract/Programmatic Sense

From a programmer's perspective, a database is a **software system** that:

- Organizes data into structured formats (tables, documents, key-value pairs, etc.)
- Provides a language or API to query and manipulate that data
- Enforces rules about how data is stored (constraints, schemas)
- Handles multiple users accessing data simultaneously
