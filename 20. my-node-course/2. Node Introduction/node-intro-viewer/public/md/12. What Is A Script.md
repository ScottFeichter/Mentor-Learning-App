# What Is A Script?

&nbsp;

A script is a file containing code that can be executed.

Scripts run from top to bottom, one line at a time.

They can do things like read files, make calculations, or start a server.

In Node, a script is simply a `.js` file that you run with the `node` command.

```bash
node myScript.js
```
