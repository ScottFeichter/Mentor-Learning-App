# Cypress Testing

## Project Structure

```
cypress-example-project/
├── package.json
├── cypress.json
└── cypress/
    └── integration/
        └── test-spec.js
```

## Setup

```bash
cd cypress-example-project
npm install
```

The first time you install Cypress, the installation could take a few minutes to complete.

After a successful installation, run tests with:

```bash
npm test
```

## Reading Cypress Test Results

- Tests are written in JavaScript
- They describe user actions and expected results

## Common Error Messages

- **Assertion Error** — HTML element cannot be found on the page, or `.should()` text doesn't match

## Running Cypress Tests

### Challenges

- Cypress can be difficult to set up initially
- Cypress can be slower to run than Mocha, especially for fails

### When to Run

- Only after completing an entire phase of the practice:
  1. Complete a single phase
  2. Check work visually
  3. Run the test to confirm solution

### Troubleshooting

- Contact instructor if issues arise
