# RITHM JAVASCRIPT QUESTIONS

5.31.22

Let has block scope and var has global scope.

- Is a let not within a block having global scope? Or cannot it not be inside an inner block.
- have the ability to be redeclared

Let

- Cannot be redeclared (in the same block)
- Must be declared before being used (not hoisted)
- Have block scope

Var

- Can be redeclared anywhere
- Has global scope

|  | Can it be Redeclared?\* | Can it be Reassigned?  | Can it be accessed before declaration?  |
| :---- | :---- | :---- | :---- |
| const | No | No | No |
| let | No | Yes | No |
| var | Yes | Yes | Yes\*\* |

Why is operator \+ shorthand for the Number function?

Why is \!\! convert to a boolean and is this changing/reassigning the value? This is coercion.

“” \= false
\[\]=true
{}=true

Why is \[\] coerced by \== into an empty string but {} is not?

Primitive Type vs Reference Type \- is the practical difference the way the values are passed or are there other considerations? Reference means the same as pointer?

Literal Array vs Constructor Array

Dynamic \- defined at run time

Static \- defined before(?) compile time (there is no compile in JavaScript correct?)

Compare top 2 cards by starting with the first letter of each card and if they are equal look at second letter etc… place first one face down in new pile. Repeat.

When one pile finishes put top card in empty stack. Compare. Repeat.

// NOTE: we are using the word property because arrays are in fact objects in JS \- is this accurate?

// PROPERTY VS FUNCTION length vs length()

// length() is a function with no pre determined action
// length property returns the length of a specified array (also works with strings)



Notes June 9th \-

var is not used much anymore should usually use let unless you know you will want or might want to reassign this later….

Const does not mean immutability \- for example if it is an array you can still push to it

Block aka local scope?  scope \- in curly braces of functions, conditionals, and loops

Function scope

Global scope \- everywhere else????

Var always has global scope unless it is declared in a function

KeywordCan ReassignCan RedeclareCan MutateScope Rulesvar✓✓✓function scopelet✓✗✓block scopeconst✗✗✓block scope

They like for…of because it is easier to read in nested arrays

**Big TakeAways from Rithm:**

Use let
Use \=== instead of \==
Use commas instead of \+ in console.log()

Always declare a variable with var, let, or const…even though it allows you to declare without
“use strict” at the top of the code is good because it will prevent bad things such as this

Use for…of as much as you can on arrays

for…in (with objects you must use for…in so not much choice)

Primitives vs Reference types
	With primitives \=== compares value and type
	With references \=== compares memory addresses

DO NOT USE \=== ON REFERENCE TYPES
