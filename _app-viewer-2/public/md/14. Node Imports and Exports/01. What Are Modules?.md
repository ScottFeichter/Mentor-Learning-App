# What Are Modules?

The module system utilized by JS allows for importing and exporting code between files.

## What Are Module


## ES Modules vs CommonJS

If you're using a `package.json` with `"type": "module"`, you're using ES modules.

Without it, Node defaults to CommonJS.

You will learn more details about this in subsequent chapters.
