# Classes

&nbsp;

As we saw in the previous page, a CIJO is built from a **Class**.

## Classes Are Blue Prints For Objects
- A CIJO will inherit essential properties from it's parent **Class**
- We can create unlimited CIJOs from a **Class**
- The method we use to instantiate an object is called the constructor
- When invoking the constructor it uses the name of the **Class**
- **Classes** can be built from parent **Classes** through Inheritance

## Class Methods

### Instance Methods

Called on an instantiated object:

```js
class Person {
  greet() { console.log('hi') }  // instance method
}

let myPerson = new Person()
myPerson.greet()  // called on the instance
```


### Static Methods

Called on the class itself, not on an instance:

```js
class Person {
  static create() { return new Person() }  // static method
}

Person.create()  // called on the class
```
