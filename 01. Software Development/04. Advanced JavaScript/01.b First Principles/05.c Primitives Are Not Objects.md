# Primitives Are Not Objects

Primitives have no properties or methods of their own.

When you call a method on a primitive like `"hello".toUpperCase()`, JS temporarily wraps it in a primitive wrapper object — then discards it.

The primitive itself is never an object.
