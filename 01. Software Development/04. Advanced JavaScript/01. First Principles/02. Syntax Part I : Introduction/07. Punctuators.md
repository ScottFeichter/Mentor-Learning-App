# Puntuators

&nbsp;

**Punctuators** are structural characters that shape the form of code.

---

## What They Are

**Punctuators** are a kind of Lexical and a sibling to Keywords.

### Parts Of Statements

**Punctuators** are building block pieces of Statements.

Sometimes **Punctuators** can stand alone as Statements but this is not useful.

### What They Are Not

**Punctuators** are not Operators since they do not act on Operands.

**Punctuators** are not Expressions because they do not create a value.

---

## JS Punctuators

Here are all the **Punctuators** in JS and their usages:

<!-- &nbsp; -->

### Curly Braces `{}`

#### Defines object bodies:
```js
const user = { name: 'Alice', age: 30 };
```

#### Defines conditional bodies:
```js
if (true) { console.log('yes'); }
```

#### Defines loop bodies:
```js
for (let i = 3; i > 0; i++) { console.log('loop body'); }
```

#### Defines function bodies:
```js
function greet() { return 'hello'; }
```

<div class="de-indent">

---

</div>
<!-- &nbsp; -->

### Parentheses `()`

#### Groups expressions to control evaluation order:
```js
const result = (2 + 3) * 4;
```

#### Wraps parameters in a function declaration:
```js
function greet(name) { return name; }
```

#### Wraps arguments in a function call:
```js
console.log('hello');
```

#### Wraps a condition and coerces truthy/falsy:
```js
if (score > 10) { console.log('high'); }
```

#### Wraps the control expression in a loop:
```js
for (let i = 0; i < 3; i++) { console.log(i); }
```

<div class="de-indent">

---

</div>
<!-- &nbsp; -->

### Brackets `[]`

#### Defines array literals:
```js
const colors = ['red', 'green', 'blue'];
```

#### Accesses array items by index:
```js
const first = colors[0];
```

#### Accesses object properties by key:
```js
const val = obj['name'];
```

<div class="de-indent">

---

</div>
<!-- &nbsp; -->

### Semicolon `;`

#### Marks the end of a statement:
```js
const x = 5;
```

<div class="de-indent">

---

</div>
<!-- &nbsp; -->

### Comma `,`

#### Separates items in an array:
```js
const nums = [1, 2, 3];
```

#### Separates parameters:
```js
function add(a, b) { return a + b; }

```

#### Separates arguments:
```js
add(1, 2);
```

#### Separates properties in an object:
```js
const point = { x: 0, y: 0 };
```

<div class="de-indent">

---

</div>
<!-- &nbsp; -->

### Dot `.`

#### Accesses a property on an object:
```js
const name = user.name;
```

#### Calls a method on an object:
```js
console.log('hello');
```



&nbsp;
&nbsp;
