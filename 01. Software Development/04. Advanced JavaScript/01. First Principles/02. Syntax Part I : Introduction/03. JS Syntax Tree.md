# JS Syntax Tree

&nbsp;

Use the arrows and buttons to explore the hierarchical relationships.

---

```json id="syntax-tree-data"
{
  "Syntax": {
    "Incidentals": {
      "Comments": {
        "//": "single line comment",
        "/* */": "multi line comment"
      },
      "Whitespace": {
        "space": "separates tokens",
        "tab": "indentation",
        "newline": "ends a line"
      }
    },
    "Statements": {
      "Lexicals": {
        "Punctuators": {
          "Grouping":         { "( )": "groups expressions, wraps arguments and parameters" },
          "Block":            { "{ }": "groups statements into a block" },
          "Array":            { "[ ]": "defines or accesses an array" },
          "Terminator":       { ";": "ends a statement" },
          "Accessor":         { ".": "accesses a property or method" },
          "Separator":        { ",": "separates items in a list" },
          "Arrow":            { "=>": "defines an arrow function" },
          "Colon":            { ":": "separates key from value, or label from statement" }
        },
        "Keywords": {
          "Declaration": {
            "var":      "declares a function-scoped variable",
            "let":      "declares a block-scoped variable",
            "const":    "declares a block-scoped constant",
            "function": "declares a function",
            "class":    "declares a class"
          },
          "Instantiation": {
            "new":     "triggers instantiation of a class, calling its constructor",
            "extends": "inherits from a parent class"
          },
          "Branching": {
            "if":      "executes a block if condition is true",
            "else":    "executes a block if condition is false",
            "switch":  "matches a value against multiple cases",
            "case":    "defines a branch in a switch",
            "default": "fallback branch in a switch if no case matches",
            "break":   "exits a switch case or loop"
          },
          "Loops": {
            "for":      "loops with an initializer, condition, and increment",
            "while":    "loops while a condition is true",
            "do":       "loops at least once, then checks condition",
            "in":       "iterates over the keys of an object (for...in)",
            "of":       "iterates over the values of an iterable (for...of)",
            "continue": "skips to the next loop iteration"
          },
          "Jump": {
            "return":   "exits a function and optionally returns a value",
            "break":    "exits a loop or switch",
            "continue": "skips to the next loop iteration"
          },
          "Error Handling": {
            "try":     "wraps code that might throw an error",
            "catch":   "handles a thrown error",
            "finally": "runs after try/catch regardless of outcome",
            "throw":   "manually throws an error"
          },
          "Type": {
            "typeof":     "returns the type of a value as a string",
            "instanceof": "checks if an object is an instance of a class",
            "void":       "evaluates an expression and returns undefined",
            "delete":     "removes a property from an object"
          },
          "Module": {
            "import":  "brings in exports from another module",
            "export":  "exposes values from a module",
            "default": "marks the default export of a module"
          },
          "Async": {
            "async": "marks a function as asynchronous",
            "await": "pauses execution until a promise resolves",
            "yield": "pauses a generator function and returns a value"
          }
        }
      },
      "Expressions": {
        "Operators": {
          "Assignment": {
            "=":  "assigns a value",
            "+=": "adds and assigns",
            "-=": "subtracts and assigns",
            "*=": "multiplies and assigns",
            "/=": "divides and assigns"
          },
          "Arithmetic": {
            "+": "addition",
            "-": "subtraction",
            "*": "multiplication",
            "/": "division",
            "%": "remainder"
          },
          "Comparison": {
            "==":  "equal in value (loose)",
            "===": "equal in value and type (strict)",
            "!=":  "not equal in value (loose)",
            "!==": "not equal in value or type (strict)",
            ">":   "greater than",
            "<":   "less than",
            ">=":  "greater than or equal",
            "<=":  "less than or equal"
          },
          "Logical": {
            "&&": "and — true if both sides are truthy",
            "||": "or — true if either side is truthy",
            "!":  "not — inverts a boolean",
            "??": "nullish coalescing — returns right side if left is null or undefined"
          },
          "Unary": {
            "typeof": "returns type as a string",
            "void":   "returns undefined",
            "delete": "removes a property",
            "!":      "negates a boolean"
          },
          "Conditional":       { "? :": "evaluates condition — returns left if true, right if false" },
          "Spread / Rest":     { "...": "spreads an iterable or collects remaining items" },
          "Optional Chaining": { "?.": "accesses a property only if the object is not null or undefined" },
          "Bitwise": {
            "&":  "bitwise and",
            "|":  "bitwise or",
            "^":  "bitwise xor",
            "~":  "bitwise not",
            "<<": "left shift",
            ">>": "right shift"
          }
        },
        "Operands": {
          "Identifiers": {
            "Variable Names":  { "score": "name given to a variable",          "userName": "camelCase is conventional" },
            "Function Names":  { "getName": "name given to a function",         "handleClick": "verb-first naming is conventional" },
            "Parameter Names": { "function greet(name)": "name is a parameter identifier" },
            "Class Names":     { "class Animal": "PascalCase is conventional for classes" },
            "Property Names":  { "user.name": "name is a property identifier on user" },
            "Label Names":     { "outerLoop:": "labels a statement for use with break or continue" }
          },
          "Values": {
            "Literals": {
              "Number":   { "42": "a numeric literal" },
              "String":   { "\"hello\"": "a string literal in double quotes" },
              "Boolean":  { "true": "boolean true literal", "false": "boolean false literal" },
              "Null":     { "null": "intentional absence of a value" },
              "Regex":    { "/pattern/flags": "a regular expression literal" },
              "Array":    { "[ contents ]": "array literal — brackets are punctuators, contents are the literal" },
              "Object":   { "{ contents }": "object literal — braces are punctuators, contents are the literal" },
              "Template": { "`hello ${name}`": "template literal — can embed expressions" }
            },
            "Evaluations": {
              "Calculations": {
                "Logical":      { "a || b": "evaluates to a or b depending on truthiness" },
                "Conditional":  { "a ? b : c": "evaluates to b if a is true, c if false" },
                "Arithmetic":   { "2 + 2": "evaluates to 4" }
              },
              "Function Calls": {
                "User Defined": { "getName()": "calls a function you wrote, evaluates to its return value" },
                "Converter":    { "Number(\"42\")": "converts a value to a different type" },
                "Constructor":  { "new Date()": "creates a new object instance" },
                "Generator":    { "Symbol('id')": "generates a new unique primitive value" }
              },
              "Retrievals": {
                "Property Access": { "user.name": "retrieves the name property from user" },
                "Index Access":    { "arr[0]": "retrieves the first item from arr" }
              },
              "Operable Keywords": {
                "this":      { "this": "resolves to the current execution context object" },
                "super":     { "super": "resolves to the parent class" }
              },
              "Operable Global Properties": {
                "undefined": { "undefined": "global property — absence of assignment" },
                "NaN":       { "NaN": "global property — result of a failed numeric conversion" },
                "Infinity":  { "Infinity": "global property — number beyond max value" },
                "-Infinity": { "-Infinity": "global property — number below min value" }
              }
            }
          }
        }
      }
    }
  }
}
```

<div class="syntax-tree" data-tree-src="syntax-tree-data"></div>
