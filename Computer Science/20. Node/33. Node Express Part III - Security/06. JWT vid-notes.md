jwt is basically a long string

json web token

use it to make routes private

when you log in you get a token

you need the token to get certain data data

to get it you must enter correct credentials

as long is token is valid we can get data (logged in?)

if a valid token is present in the request the user can get specific data

login is not a restricted route because anyone can try to log in

once logged in can use token



authentication is registration and log in but you treat them separately?


public routes vs private routes

private reqs and ress have a jwt
the tokens much match

jwt is just a way to exchange data between entities

jwt is better than a random string because it uses hashing
- http is a stateless protocol
- requests are not remembered by either side

jwt.io


"jwt can be verified and trusted because it is digitally signed"

the signing = secret and the algorithm




COMPONENTS OF JWT

jwt header: contains type of token and algorithm
            the information gets encoded ie Base64Url

payload: contains the claims which are statements about the entity
         ie userId
         the information gets encoded ie Base64Url


signature: contains the encoded header, the encoded payload, a secret (should store it in .env), an algorithm specified in the header



jsonwebtoken package via npm install jsonwebtoken this is used to sign jwts 


Authorization header in the login request using the Bearer schema.

token get's sent back and put in local storage
then when you make another request you send back a token
