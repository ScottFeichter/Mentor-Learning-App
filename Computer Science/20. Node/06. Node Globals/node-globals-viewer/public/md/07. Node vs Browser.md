# Node vs Browser

&nbsp;

Node and Browser are different environments therefore Node has some different globals compared to the Browser.

Browser has the global called "window" — Node does not.

Node has the global called "global" — the Browser does not.
