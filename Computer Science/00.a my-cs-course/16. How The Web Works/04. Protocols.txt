There are several protocols for the internet to function as it does

TRANSPORT PROTOCOLS

TCP - Transmission Control Protocol
  - establishes a connection between two sockets
  - data is sent as ordered segments
  - the receiver will ask the transmitter to re-send the missing segment
  - data cannot be lost making TCP a reliable protocol
  - the trade off is it may take longer
  - common use cases include:
        1. Web Browsing
        2. CRUD Operations
        3. File Transfer

UDP - User Datagram Protocol
  - does not establish a connection
  - data is sent unordered
  - does not provide verification data received
  - if some data is lost  is ok therefore UDP is an unreliable protocol
  - trades reliability for speed
  - common use cases include:
        1. Real Time Video
        2. Voice Over IP
        3. DNS

INTERNET PROTOCOL

IP - Internet Protocol
  - The protocol responsible for the end to end nature of joined networks

QUIK

HTTP

HTTPS
